/* Cat name during RESULTS phase - positioned differently to make room for coin text */
.stage-base.results-mode .cat-name {
    top: 50%; /* Move to middle of brown box in results phase */
    transform: translate(-50%, -50%); /* Center both horizontally and vertically */
    font-size: 24px; /* Slightly smaller in results to fit with coin text */
}

/* Username positioning - move lower to accommodate cat name at top */
.username {
    position: absolute;
    bottom: 5px; /* Keep at bottom of brown box */
    left: 50%;
    transform: translateX(-50%);
    width: 85%;
    max-width: 160px;
    
    font-family: var(--font-main);
    font-size: 20px;
    color: var(--color-black);
    text-align: center;
    font-weight: normal;
    line-height: 1.1;
    
    /* Text containment */
    word-wrap: break-word;
    overflow-wrap: break-word;
    overflow: hidden;
    box-sizing: border-box;
    
    /* Sharp pixel font rendering */
    -webkit-font-smoothing: none;
    -moz-osx-font-smoothing: unset;
    font-smooth: never;
}

/* Ensure coin reward text positioning is consistent */
.coin-reward-text {
    position: absolute;
    top: 8px; /* Top of brown box */
    left: 50%;
    transform: translateX(-50%);
    width: 85%;
    max-width: 160px;
    
    font-family: var(--font-main);
    font-size: var(--font-size-xs); /* 36px - largest text */
    color: var(--color-white);
    text-align: center;
    font-weight: normal;
    line-height: 1.1;
    
    /* Text containment */
    word-wrap: break-word;
    overflow-wrap: break-word;
    overflow: hidden;
    box-sizing: border-box;
    
    /* Sharp pixel font rendering */
    -webkit-font-smoothing: none;
    -moz-osx-font-smoothing: unset;
    font-smooth: never;
    
    /* Animation */
    animation: coinTextAppear 1s ease-out 1.2s both;
}